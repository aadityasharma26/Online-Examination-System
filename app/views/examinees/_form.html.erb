<%= form_with(model: examinee, local: true) do |form| %>
  <% if examinee.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(examinee.errors.count, "error") %> prohibited this examinee from being saved:</h2>

      <ul>
      <% examinee.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="col-sm-offset-2 col-sm-10">
    <%= form.submit "New Examinee", class: 'btn btn-primary btn-lg' %>
  </div>
<% end %>
