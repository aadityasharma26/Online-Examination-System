<%= form_with(model: subcourse, local: true) do |form| %>
  <% if subcourse.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(subcourse.errors.count, "error") %> prohibited this subcourse from being saved:</h2>

      <ul>
      <% subcourse.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
